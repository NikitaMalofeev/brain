# Активные задачи проекта "Brain Programming"

## 🎯 Приоритетные задачи

### 🟢 **[ИСПРАВЛЕНО]** Циклическая зависимость в VideoPlayer (бесконечный play/pause)
- 🟢 Исправлена циклическая зависимость между событиями KinescopePlayer и состоянием PlayerContext
- 🟢 Устранено бесконечное переключение между play/pause при включении видео
- 🟢 Разорван цикл: handlePlay/handlePause ↔ useEffect ↔ playerRef.current.play/pause()

**ДИАГНОСТИКА ПРОБЛЕМЫ:**
1. **События KinescopePlayer (onPlay/onPause)** → вызывали handlePlay/handlePause
2. **handlePlay/handlePause** → обновляли state.playing через play()/pause()
3. **useEffect на state.playing** → вызывал playerRef.current.play/pause()
4. **playerRef.current.play/pause()** → генерировал новые события onPlay/onPause
5. **ЦИКЛ ЗАМЫКАЛСЯ** → бесконечное переключение

**РЕАЛИЗОВАННОЕ РЕШЕНИЕ:**
- ✅ **Флаг programmaticChange**: Добавлен в PlayerState для различения программных изменений
- ✅ **Обновлены методы play/pause**: Устанавливают флаг programmaticChange с таймаутом сброса
- ✅ **Модифицированы обработчики**: handlePlay/handlePause игнорируют события при programmaticChange
- ✅ **Предотвращена циклическая зависимость**: События от useEffect не вызывают обработчики
- ✅ **Исправлен сброс прогресса**: setContentId теперь сбрасывает currentTime при смене контента
- ✅ **Улучшен UX**: Новое видео всегда начинается с 0:00, а не с прогресса предыдущего
- ✅ **Сохранена функциональность**: Пользовательские действия работают как прежде

### 🟢 **[ИСПРАВЛЕНО]** Отключена проблемная логика fullscreen-extra-padding
- **ПРОБЛЕМА**: Старший разработчик указал что проблема в fullscreen-extra-padding
- **ПРИЧИНА**: Дублирующая логика в AppWrapper.tsx с установкой --fullscreen-extra-padding
- **РЕШЕНИЕ**: Полностью отключить всю логику с fullscreen-extra-padding
- 🟢 Убрана вся логика viewport_changed из AppWrapper.tsx
- 🟢 Удалена CSS переменная --fullscreen-extra-padding из index.css
- 🟢 Очищены комментарии и ссылки на fullscreen-extra-padding в Page.tsx
- 🟢 Убран запрос web_app_request_viewport

**ВЫПОЛНЕННЫЕ ИСПРАВЛЕНИЯ:**
1. ✅ **AppWrapper.tsx**: Удален обработчик viewport_changed полностью
2. ✅ **AppWrapper.tsx**: Убран postEvent('web_app_request_viewport') из интервала
3. ✅ **index.css**: Удалена CSS переменная --fullscreen-extra-padding
4. ✅ **Page.tsx**: Очищены комментарии с fullscreen-extra-padding
5. ✅ **Page.tsx**: Убран postEvent('web_app_request_viewport') из useEffect
6. ✅ Система теперь использует только --safe-area-top без дополнительных отступов
7. 🧪 **ТЕСТ**: Отключена установка --safe-area-top в AppWrapper.tsx для тестирования фиксированных отступов
8. 🧪 **ТЕСТ**: Убран pt-12 из MainPage.tsx - контент теперь БЕЗ отступов сверху (экстремальное тестирование)
9. 🔍 **НАЙДЕН ИСТОЧНИК БЕЛОЙ ПОЛОСКИ**: SafeAreaFade создает белый градиент 100px сверху
10. 🧪 **ТЕСТ**: Отключен SafeAreaFade для MainPage через showSafeAreaFade={false}
11. 🔍 **НАЙДЕН ИСТИННЫЙ ИСТОЧНИК**: CSS класс .page-container имел padding-top: calc(...) = 110px!
12. ✅ **ОТКЛЮЧЕН**: Закомментирован padding-top в .page-container (index.css)
13. ✅ **ЗАЧИЩЕНЫ ВСЕ БЕЛЫЕ ФОНЫ**: Page.tsx, Page.css, убраны все backgroundColor: '#ffffff'
14. ✅ **ДОБАВЛЕН КОНТРОЛЬ ФОНА**: В Page компонент добавлен prop backgroundColor с умолчанием '#ffffff'
15. ✅ **MAINPAGE ПРОЗРАЧНАЯ**: MainPage использует backgroundColor="transparent"
16. 🎉 **РЕЗУЛЬТАТ**: Белая полоска исчезла, обычные страницы имеют белый фон, MainPage - прозрачная

### 🟡 **[КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ]** Отступы в fullscreen режиме Telegram (Уверенность: 10/10)

### 🟡 **[UX/UI]** Сплэш-скрин с 3D мозгом (IFrame интеграция)
- 🟢 Создать компонент `IFrameSplash.tsx` для отображения 3D мозга
- 🟢 Интегрировать сплэш-скрин в корневой компонент приложения
- 🟢 Добавить CSS анимацию typewriter для текста "Brain Programming → Activated"
- 🟢 Настроить блокировку swipe-навигации для Telegram Web App
- 🟢 Добавить типизацию TypeScript для Telegram Web App API
- 🟢 Исправить логику показа только при запуске приложения (не при refresh)
- 🟢 Скрыть TabBar во время показа сплэш-скрина
- 🟢 Изменить логику: сплэш-скрин вместо "Загрузка..."
- 🟢 Исправить размеры мозга для корректного отображения в экране
- 🟢 Убрать typewriter анимацию (на сайте уже есть печать текста)
- 🔴 Протестировать работу iframe с внешним доменом (https://3dbrain-three.vercel.app/)

**Детали реализации:**
- Используется готовый билд 3D мозга через iframe
- Таймер показа: строго 5 секунд (без возможности прерывания)
- **Показывается вместо стандартной загрузки:** заменяет экран "Загрузка..."
- **Показывается только при запуске приложения:** использует `sessionStorage` для отслеживания
- Не показывается при обновлении страницы (F5/Ctrl+R)
- Показывается всем пользователям во время инициализации данных
- **Корректные размеры:** мозг помещается в экран с aspect-ratio и центрированием
- **Без анимации текста:** убрана typewriter анимация (на сайте уже есть печать)
- Позиционирование: `position: fixed` поверх всего приложения
- Sandbox режим: `allow-scripts` (без user-инпута)
- Поддержка accessibility: `aria-hidden="true"`
- Интеграция с Telegram Web App (блокировка свайпов во время показа)

### 🟢 **[UX/UI]** Динамическое название ступени в профиле
- 🟢 Заменить статический текст "исцеление" на динамическое название текущей ступени
- 🟢 Добавить fallback для случая когда название ступени не найдено  
- 🟢 Протестировать отображение с разными названиями ступеней

**Детали реализации:**
- В `UserPage.tsx` (строка 207) заменить хардкод `"исцеление"` на `stages?.[currentLevel - 1]?.name`
- Использовать существующие данные из хука `useLibraryStages` 
- Добавить fallback на "Неизвестная ступень" если название не найдено
- Сохранить uppercase стиль для названия ступени
- Учесть что `currentLevel` начинается с 1, а массив с 0

### 🟡 **[UX]** Информативность и безопасность  
- 🔴 Добавить информативные алерты при удалении сущностей
- 🔴 Добавить алерты при выходе без сохранения изменений

### 🟢 **[ФУНКЦИОНАЛЬНОСТЬ]** Динамическая загрузка материалов в MaterialPage
- 🟢 Заменить статические данные на динамическую загрузку материала по ID из URL
- 🟢 Убрать хардкод аудио URL в NewPlayer компоненте
- 🟢 Заменить статическое обращение к blocks[0] на реальные данные материала  
- 🟢 Добавить обработку состояний загрузки и ошибок
- 🟢 Использовать существующие хуки для работы с материалами

**ВЫПОЛНЕННЫЕ ИСПРАВЛЕНИЯ:**
- ✅ Заменен хардкод аудио URL на `firstBlock.content_url`
- ✅ Добавлены проверки на существование данных (data, blocks)
- ✅ Улучшена обработка ошибок с информативными сообщениями
- ✅ Добавлена безопасная работа с firstBlock вместо blocks[0]
- ✅ Улучшена типизация и добавлены key для map элементов
- ✅ Добавлен правильный alt текст для изображений
- ✅ Код стал более читаемым и безопасным

**РЕЗУЛЬТАТ:** Теперь MaterialPage динамически загружает реальные данные материала по ID из URL, включая корректные аудио URLs из базы данных.

### 🟢 **[ФИНАЛЬНАЯ ДОРАБОТКА]** Timing проблема с WaveSurfer в MaterialPage
- 🟢 Исправить AbortError при уничтожении WaveSurfer в NewPlayer компоненте
- 🟢 Исправить проблему с загрузкой аудио в MaterialPage (buildFileUrl исправление)
- 🟢 Добавить безопасные проверки для WaveSurfer lifecycle
- 🟢 Исправить timing проблему: волна загружается после первоначальной ошибки

**ПОЛНОСТЬЮ ИСПРАВЛЕНО:**
- ✅ **ГЛАВНАЯ ПРОБЛЕМА РЕШЕНА**: Аудио теперь загружается благодаря `buildFileUrl()`
- ✅ Добавлены try-catch блоки для безопасной инициализации WaveSurfer
- ✅ Добавлены проверки на существование audioUrl и контейнера
- ✅ Безопасная очистка с остановкой воспроизведения перед destroy()
- ✅ Добавлен error handler для событий WaveSurfer
- ✅ **TIMING ПРОБЛЕМА РЕШЕНА**: Заменен useEffect на useLayoutEffect
- ✅ Добавлена проверка готовности DOM контейнера (offsetWidth > 0)
- ✅ Добавлен timeout для инициализации когда контейнер не готов
- ✅ Добавлен isMountedRef для предотвращения инициализации после размонтирования
- ✅ Убраны debug логи из MaterialPage

**РЕЗУЛЬТАТ:** 
Аудиоплеер в MaterialPage теперь работает стабильно без ошибок инициализации. WaveSurfer создается только когда DOM контейнер полностью готов и имеет размеры.

### 🟢 **[ИСПРАВЛЕНИЕ]** React DOM предупреждение в PlayIcon
- 🟢 Исправить неправильный SVG атрибут `stop-opacity` на `stopOpacity` в PlayIcon компоненте
- 🟢 Найти и исправить все подобные проблемы с SVG атрибутами в проекте

**ВЫПОЛНЕННЫЕ ИСПРАВЛЕНИЯ:**
- ✅ Заменен `stop-color` на `stopColor` в PlayIcon
- ✅ Заменен `stop-opacity` на `stopOpacity` в PlayIcon  
- ✅ Проверен весь проект - других аналогичных проблем не найдено
- ✅ React DOM предупреждения больше не появляются

**РЕЗУЛЬТАТ:** Устранены все React DOM предупреждения в SVG компонентах, код соответствует JSX стандартам.

### 🟢 **[ИСПРАВЛЕНИЕ]** Статус урока на LessonPage при админском завершении
- 🟢 Исправить логику определения статуса урока в `getLessonPageStatus()` на странице урока
- 🟢 Добавить приоритет для `lesson_progress.is_completed` независимо от наличия задания
- 🟢 Устранить проблему отображения "Просрочено" для уроков, завершенных через админку

**ПРОБЛЕМА:**
Когда администратор вручную завершает урок через админку (устанавливает `lesson_progress.is_completed = true`), на странице урока всё равно показывался статус "Просрочено" или "Ожидайте результата проверки", игнорируя флаг завершенности.

**ВЫПОЛНЕННЫЕ ИСПРАВЛЕНИЯ:**
- ✅ Изменена логика `getLessonPageStatus()` в `LessonPage.tsx`
- ✅ **ПРИОРИТЕТ 0**: Добавлена проверка `isLessonCompleted` как самый высокий приоритет
- ✅ Переупорядочены приоритеты для обеих веток (с заданием и без задания)
- ✅ Убрана дублирующая проверка `isLessonCompleted` для уроков без задания
- ✅ Обновлена нумерация приоритетов для ясности кода

**РЕЗУЛЬТАТ:** 
Теперь если урок завершен через админку (`lesson_progress.is_completed = true`), он корректно отображается со статусом "Завершено" ✅ независимо от наличия задания, дедлайнов или статуса submission.

**ДОПОЛНИТЕЛЬНОЕ ИСПРАВЛЕНИЕ:**
- ✅ Исправлена аналогичная проблема в `LessonCard.tsx` - функция `getLessonStatus()`
- ✅ Приоритет проверки `lesson.is_completed` поднят выше дедлайнов и submission статусов
- ✅ Убрана дублирующая проверка завершенности в логике дедлайнов

**КОРНЕВАЯ ПРИЧИНА НАЙДЕНА И ИСПРАВЛЕНА:**
- ✅ **ГЛАВНАЯ ПРОБЛЕМА**: В `useStageDetails.ts` логика определения `isCompleted` НЕ учитывала поле `is_completed` из `lesson_progress`!
- ✅ Добавлен запрос поля `is_completed` в SQL-селект из таблицы `lesson_progress`
- ✅ Обновлена мапа прогресса для включения флага `is_completed`
- ✅ **ПЕРЕПИСАНА ЛОГИКА**: Теперь `progress.is_completed` имеет приоритет 1 (покрывает админское управление)
- ✅ Сохранена обратная совместимость: approved submissions и completed_at также засчитываются как завершение
- ✅ Теперь карточки уроков на странице ступени корректно показывают статус "Завершено"

**ИСПРАВЛЕНИЕ UI FLICKERING:**
- ✅ **ПРОБЛЕМА**: При загрузке урока сначала показывался статус "Просрочено", затем резко менялся на "Завершено"
- ✅ Добавлено состояние `userDataLoading` в `LessonPageState` для отслеживания загрузки пользовательских данных
- ✅ Показывается плейсхолдер "Загрузка..." пока не загружены `progress` и `submission`
- ✅ Исправлена инициализация состояния с корректными fallback значениями
- ✅ Предотвращен UI flickering - статус отображается только после полной загрузки данных
- ✅ Улучшен UX: плавное отображение корректного статуса без промежуточных мерцаний

### 🟡 **[ФУНКЦИОНАЛЬНОСТЬ]** Drag-and-drop сортировка для материалов в библиотеке
- 🟢 Создать компонент `DraggableMaterialRow.tsx` для строки материала.
- 🟢 Реализовать логику drag-and-drop (нативный HTML5).
- 🟢 Добавить в `MaterialsManager.tsx` функцию `handleMaterialReorder` для обновления порядка в базе данных.
- 🟢 Интегрировать `DraggableMaterialRow` в таблицу материалов.
- 🟡 Протестировать функционал.

### 🟡 **[НОВАЯ ФУНКЦИЯ]** Загрузка аватара куратора с кадрированием (КОНСИСТЕНТНАЯ РЕАЛИЗАЦИЯ)
- 🟢 **Цель:** Дать администратору возможность загружать и кадрировать квадратную фотографию для аватара куратора.
- 🟢 **Архитектура (КОНСИСТЕНТНАЯ С ПРОЕКТОМ):**
  - 🟢 **Модальное окно аватара:** Создан отдельный компонент `CuratorAvatarModal.tsx` (консистентно с архитектурой проекта)
  - 🟢 **Использовать существующий FileUploader:** Переиспользован компонент `@/components/FileUploader/FileUploader` с правильными пропсами
  - 🔴 **Cloudflare R2 интеграция:** Использованы `buildImageUrl()`, `uploadFileToR2()` и `FILE_PREFIXES.IMAGE` из `@/lib/cloudflareR2Service`
  - 🟢 **Поле БД:** Подготовлено для использования существующего поля `users.photo_url` (НЕ создается новое `avatar_url`)
- 🟢 **UI Компоненты (ТОЧЬ-В-ТОЧЬ КАК В МАТЕРИАЛАХ):**
  - 🟢 Кнопка аватара в таблице кураторов для открытия модального окна (плейсхолдер с инициалами)
  - 🟢 Модальное окно с заголовком "Аватар куратора: {curator_name}"
  - 🟢 FileUploader с `acceptedTypes="image/*"` и `filePrefix="images/"`
  - 🟢 Кнопки "Сохранить аватар", "Удалить аватар", "Отмена" (те же стили что в MaterialsManager)
- 🟢 **Кадрирование изображения:**
  - 🟢 Установить библиотеку `react-image-crop`: `npm install react-image-crop`
  - 🟢 После выбора файла в FileUploader показать интерфейс кропа с фиксированным aspect ratio 1:1 (квадрат)
  - 🟢 Пользователь выбирает квадратную область → генерируется обрезанный canvas → конвертируется в Blob → загружается в R2
- 🟢 **Логика сохранения (КАК В MATERIALMANAGER):**
  - 🟢 Добавлены мутации `updateCuratorAvatar` и `deleteCuratorAvatar` в хук `useCuratorsAdmin`
  - 🟢 Обновлен тип `Curator` с полем `photo_url`
  - 🟢 Обновлен SQL-запрос для загрузки `photo_url` из базы данных
  - 🟢 Реализована логика сохранения через прямое обновление `users.photo_url`
  - 🟢 Автоматическое обновление локального состояния через React Query invalidation
- 🟢 **Отображение в таблице:**
  - 🟢 Добавлена колонка "Аватар" в таблицу кураторов
  - 🟢 Реализован круглый аватар 40x40px с плейсхолдером-инициалами
  - 🟢 При клике на аватар открывается модальное окно редактирования

---

## 🎨 UI/UX доработки

### 🟢 **[UX]** Исправление грамматики в UI
- 🟢 Исправить склонение слова "задание" в зависимости от числа на странице ступени и в прогресс-баре.
- 🟢 Исправить склонение в заголовке прогресс-бара ("Выполнено X заданий").

### 🟡 **[Дизайн]** Соответствие Figma
- 🟡 Последняя карточка при нечетном количестве - на два столбца
- 🟡 Проверить выравнивание label "ступень" под карточкой
- 🔴 Финальное тестирование соответствия дизайну Figma

### 🟡 **[Тестирование]** Адаптивность и производительность
- 🟡 Тестирование адаптивности на разных размерах экрана
- 🟡 Протестировать работу обложек уроков в браузере
- 🟡 Протестировать полный флоу: урок → завершение → разблокировка следующего

---

## 🚀 Технические улучшения

### 🟢 **[Рефакторинг]** Вынести функцию плюрализации в хелпер
- 🟢 Создать новый файл `src/helpers/pluralize.ts`.
- 🟢 Перенести функцию `getNounPluralForm` в новый файл.
- 🟢 Обновить импорты в `StagePage.tsx` и `UserProgress.tsx`.

### 🟡 **[Оптимизация]** Очистка кода
- 🔴 Удалить неиспользуемый AudioPlayer.tsx
- 🔴 Очистить неиспользуемые CSS стили из Player.css

---

## ✅ Недавно выполненные задачи

### 🟢 **[ИСПРАВЛЕНИЕ]** Правильный подсчет НЕоткрытых уроков до следующей ступени

**ПРАВИЛЬНОЕ понимание задачи:**
- Нужно считать количество уроков, которые еще НЕ ОТКРЫТЫ (open_at > NOW() или open_at IS NULL но не выполнены условия открытия)
- Это НЕ связано с выполнением/невыполнением урока (is_completed)
- Урок может быть открыт (open_at <= NOW()), но не завершен (is_completed = false)
- Нам нужно показать сколько уроков еще заблокированы до следующей ступени

**ПРОБЛЕМА С ДАННЫМИ:**
- В таблице `lessons` НЕТ поля `is_unlocked`
- Разблокировка урока определяется по полю `open_at` (если NULL или <= NOW(), то урок открыт)
- Но функция `get_library_stages` возвращает только агрегированные данные по ступеням
- Для подсчета неоткрытых уроков нужны индивидуальные данные о каждом уроке

**ЧТО нужно исправить:**
- Либо создать новую RPC функцию, которая возвращает количество неоткрытых уроков
- Либо изменить подход и использовать существующие данные из `useStageDetails` 
- В данный момент невозможно правильно подсчитать неоткрытые уроки только по `get_library_stages`

**ПЛАН РЕШЕНИЯ (ВЫПОЛНЕНО):**
1. ✅ В `StagePage.tsx` - исправлена логика подсчета неоткрытых уроков в текущей ступени
2. ✅ Создана SQL миграция для обновления `get_library_stages` - добавлено поле `unlocked_lessons`
3. ✅ Обновлен тип `StageProgressData` в `UserProgress.tsx` 
4. ✅ Заменена логика подсчета в `UserProgress.tsx` на использование `unlocked_lessons`

**ПРЕИМУЩЕСТВА:**
- Переиспользуем существующую RPC функцию
- Добавляем только одно поле в возвращаемые данные
- Быстро и надежно

### 🟢 **[UX/UI]** Динамический подсчет прогресса до следующей ступени (НЕПРАВИЛЬНО ВЫПОЛНЕНО)
- 🔴 ОШИБКА: Считал незавершенные уроки вместо неоткрытых
- 🔴 Нужно переделать логику подсчета
- 🟢 Заменен хардкод "24 дня" на динамический подсчет
- 🟢 Обновлен компонент `UserProgress` для отображения реального количества заданий  
- 🟢 Обновлена страница `StagePage` для показа прогресса до следующей ступени
- 🟢 Добавлена логика подсчета незавершенных уроков в открытых ступенях
- 🟢 Улучшены сообщения о прогрессе с учетом различных сценариев (все ступени пройдены, готовность к разблокировке и т.д.)

---

 ## 🛠️ **[Рефакторинг]** Декомпозиция гигантского AdminPage.tsx

**Цель:** Уменьшить размер файла `AdminPage.tsx` (сейчас >2000 строк), повысить читаемость и поддерживаемость кода путем разделения на модули и хуки.

### **Этап 1: Декомпозиция компонентов (Вынос в отдельные файлы)**
- 🔴 **CoursesManager:**
  - 🔴 Создать директорию `src/pages/AdminPage/components/CoursesManager/`.
  - 🔴 Создать файл `CoursesManager.tsx` внутри новой директории.
  - 🔴 Перенести компонент `CoursesManager` и его пропсы (`CoursesManagerProps`) из `AdminPage.tsx` в новый файл.
  - 🔴 Добавить необходимые импорты (`React`, `useState`, `useCoursesAdmin`) в `CoursesManager.tsx`.
  - 🔴 Заменить код компонента в `AdminPage.tsx` на импорт из нового файла.
- 🔴 **StagesManager:**
  - 🔴 Создать директорию `src/pages/AdminPage/components/StagesManager/`.
  - 🔴 Создать файл `StagesManager.tsx` внутри.
  - 🔴 Перенести компонент `StagesManager` и `StagesManagerProps` из `AdminPage.tsx`.
  - 🔴 Добавить необходимые импорты в `StagesManager.tsx`.
  - 🔴 Заменить код компонента в `AdminPage.tsx` на импорт.
- 🔴 **TariffLimitsSection:**
  - 🔴 Создать файл `TariffLimitsSection.tsx` в `src/pages/AdminPage/components/StagesManager/`.
  - 🔴 Перенести компонент `TariffLimitsSection` и `TariffLimitsSectionProps` из `AdminPage.tsx`.
  - 🔴 Добавить импорты (`React`, `useState`, `useEffect`, `useTariffLimits`) в `TariffLimitsSection.tsx`.
  - 🔴 Импортировать `TariffLimitsSection` в `StagesManager.tsx`.
- 🔴 **BlocksManager:**
  - 🔴 Создать директорию `src/pages/AdminPage/components/BlocksManager/`.
  - 🔴 Создать файл `BlocksManager.tsx` внутри.
  - 🔴 Перенести компонент `BlocksManager` и `BlocksManagerProps`.
  - 🔴 Добавить импорты в `BlocksManager.tsx`.
  - 🔴 Заменить код компонента в `AdminPage.tsx` на импорт.
- 🔴 **Breadcrumb:**
  - 🔴 Создать директорию `src/pages/AdminPage/components/Breadcrumb/`.
  - 🔴 Создать файл `Breadcrumb.tsx` внутри.
  - 🔴 Перенести компонент `Breadcrumb` и `BreadcrumbProps`.
  - 🔴 Заменить код компонента в `AdminPage.tsx` на импорт.
- 🔴 **Итоговая зачистка Этапа 1:**
  - 🔴 Проверить и удалить все перенесенные компоненты и их пропсы из `AdminPage.tsx`.
  - 🔴 Убедиться, что все новые компоненты правильно импортированы и приложение работает.

### **Этап 2: Централизация типов**
- 🔴 Создать файл `src/pages/AdminPage/types.ts`.
- 🔴 **Вынести типы:**
    - 🔴 Перенести `SupabaseUser` в `types.ts` и экспортировать.
    - 🔴 Перенести `NavigationState` в `types.ts` и экспортировать.
    - 🔴 Перенести `SubmissionsNavigationState` в `types.ts` и экспортировать.
    - 🔴 Перенести `BlockModalData` в `types.ts` и экспортировать.
- 🔴 **Обновить импорты:**
    - 🔴 В `AdminPage.tsx` заменить локальные типы на импорты из `src/pages/AdminPage/types.ts`.
    - 🔴 В `Breadcrumb.tsx` обновить импорт `NavigationState`.
    - 🔴 В `BlocksManager.tsx` обновить импорт `BlockModalData`.

### **Этап 3: Инкапсуляция логики в кастомные хуки**
- 🔴 **Создать директорию для хуков:** `src/pages/AdminPage/hooks/`.
- 🔴 **`useAdminAuth` Hook:**
  - 🔴 Создать файл `useAdminAuth.ts` в новой директории.
  - 🔴 Перенести всю логику аутентификации: `useState` для `passwordAuth`, `password`, `error`, `adminUser`, `login`, `authLoading`.
  - 🔴 Перенести `useEffect` для валидации localStorage.
  - 🔴 Перенести функции `authenticateUser` и `handleLogout`.
  - 🔴 Хук должен возвращать объект со всеми состояниями и функциями.
  - �� В `AdminPage.tsx`