### 🚀 Что делаем дальше — фин-план по токенам

> TL;DR: Добавляем таблицу `access_tokens`, атомарно активируем её через Edge-функцию, даём админам batch-генерацию и копирование ссылки формата
> `https://t.me/<bot>?startapp=<token>`.
> Мигрируем существующих участников напрямую в `user_course_enrollments`, без токенов.

---

## 1. Схема данных

```sql
create table access_tokens (
    id                   uuid primary key default gen_random_uuid(),
    token                text not null,                -- base62, 16 симв.
    course_id            uuid references courses(id),
    tariff_id            uuid references tariffs(id),  -- тариф ± 1 поток сейчас
    status               text not null default 'created' check (status in ('created','used')),
    used_by_user_id      uuid references users(id),
    used_at              timestamptz,
    created_by_user_id   uuid references users(id),
    comment              text,
    created_at           timestamptz default now(),
    tg_id                bigint null                   -- ✅ НОВОЕ: Для персональных токенов
);
create unique index access_tokens_token_uidx on access_tokens(token);
create index access_tokens_tg_id_idx on access_tokens(tg_id) where tg_id is not null; -- ✅ НОВОЕ
```

*Таблицы `tariffs`, `courses`, `users` и `user_course_enrollments` уже описаны в PRD* .

---

## 2. Потоки активации

### Поток 1: Обычный токен (через ссылку) - как было
1. **Админ** генерит токен/пакет токенов и сразу копирует ссылку вида `https://t.me/OUR_BOT?startapp=<token>`.
2. **Покупатель** кликает эту ссылку — Telegram сразу открывает Mini-App.
3. В Mini-App через `WebApp.initData` (`start_param`) получаем токен, вызываем RPC-функцию `redeem_access_token`.
4. В транзакции:
   - проверяем `access_tokens` (`status='created'`);
   - делаем `UPDATE … status='used', used_by_user_id=?, used_at=now()`;
   - пишем запись в `user_course_enrollments` + `tariff_id` в `user_tariffs`.

### Поток 2: Персональный токен (автоматический) - ✅ НОВОЕ
1. **Админ** в админке создает персональный токен, указывая `Telegram ID` пользователя, курс и тариф.
2. Вызывается RPC-функция `assign_access_token_to_user`, которая:
   - Создает токен в `access_tokens` с указанным `tg_id`.
   - **Немедленно** активирует этот токен для пользователя.
   - Зачисляет пользователя на курс и назначает тариф. Ссылка не генерируется и не нужна.
3. **Пользователь** просто заходит в приложение.
4. Mini-App при инициализации, если не находит `start_param`, делает вызов RPC `find_token_by_tg_id`.
5. Если находится неиспользованный персональный токен, он автоматически применяется.

---

## 2.1. Обработка ошибок в Mini-App

В случае, если токен невалиден (не найден, уже использован, истёк срок и т.д.), Mini-App должно показать пользователю отдельную страницу с информацией:
*   **Заголовок:** "Ошибка активации"
*   **Текст:** "К сожалению, этот код доступа недействителен. Пожалуйста, проверьте правильность ссылки или обратитесь в поддержку."
*   **Кнопка:** "Написать в поддержку" (ведёт на Telegram-чат поддержки).

---

## 3. Генерация токена

* **Формат.** `base62(16)` ≈ 95 бит энтропии, короче 64-символьного лимита Telegram.
* **Храним в plain-text.** Хеш не нужен (нет повторного использования, RLS ограничит SELECT).
* **Batch.** Запрос принимает `count` + `tariff_id`; возвращает массив строк-ссылок.

---

## 4. Изменения в админ-панели

| Экран «Токены» | Функционал                                                          |
| -------------- | ------------------------------------------------------------------- |
| **Форма создания** | ✅ **НОВОЕ:** Переключатель "Обычные" / "Персональные". Поле "Telegram ID" для персональных. |
| Таблица        | ✅ **НОВОЕ:** Колонка "Тип токена" ("Обычный" / "Персональный (TG: id)"). Обновленные статусы ("Активирован автоматически"). |
| Фильтры        | по статусу, тарифу, дате, ✅ **типу токена**.                                            |
| Действия       | **Скопировать ссылку** (только для обычных), Batch-создание, Export CSV, **Назначить тариф** (для персональных). |

**UI для Batch-создания:**
*   Выпадающий список для выбора **Курса**.
*   Выпадающий список для выбора **Тарифа** (фильтруется по выбранному курсу).
*   Поле для ввода количества токенов.
*   ✅ **НОВОЕ:** Поле для ввода Telegram ID (для персональных токенов, `count` = 1).
*   Кнопка "Сгенерировать ссылки" / "Назначить тариф".

---

## 5. Безопасность & RLS

```sql
-- Только сервис-роль и роли 'admin'/'curator' могут INSERT/SELECT/UPDATE
create policy "tok_admin" on access_tokens
  for all using (auth.role() in ('service_role','admin','curator'));
-- Обычному участнику SELECT запрещён.
```
Эта политика остается актуальной.

---

## 6. Telegram-бот (Фолбэк-механизм)

* `/start <token>` обработчик (является второстепенным, основной флоу - через `startapp`):

  * проверяет формат токена (16-char base62);
  * делает быстрый вызов RPC-функции через Supabase, получает результат `{ok|error}`;
  * шлёт кнопку `web_app` (token в URL) или сообщение об ошибке («токен недействителен / уже использован»).
* **Inline-mode** не нужен.

---

## 7. Миграция существующих участников

```sql
insert into user_course_enrollments (user_id, course_id, enrollment_date, status)
select u.id, <course_id>, now(), 'active'
from profiles u
where u.tariff_id is not null;
```

Токены им **не** создаём; Mini-App пустит их по уже прописанному `tariff_id`.

---

## 8. Текущее состояние реализации (Январь 2025)

### ✅ Что УЖЕ реализовано:

**Frontend (Mini-App):**
- **Проверка токена из startParam** - при запуске приложения извлекается токен из Telegram launch параметров
- **Логика обработки токена** - вызов RPC функции `redeem_access_token` с токеном
- **✅ НОВОЕ: Поиск персонального токена** - если `start_param` отсутствует, приложение ищет токен по `tg_id` пользователя.
- **Проверка активного тарифа** - через хук `useActiveTariff` проверяется наличие активного тарифа у пользователя
- **Контроль доступа**: 
  - Если нет активного тарифа И нет токена → показ `TokenErrorPage`
  - Если есть токен → попытка активации, при ошибке → показ `TokenErrorPage` с деталями ошибки
  - Если есть активный тариф → доступ к приложению
- **Обработка ошибок** - показ `TokenErrorPage` с сообщением об ошибке и кнопкой "Написать в поддержку"
- **Инвалидация кэша** - после успешной активации токена обновляется информация о пользователе и тарифах

**Admin Panel (Генерация токенов):**
- **✅ НОВЫЙ Интерфейс создания токенов** - форма с выбором типа токена (обычный/персональный), курса, тарифа, количества токенов и Telegram ID.
- **Конфигурация бота** - название бота подтягивается из `BOT_CONFIG.BOT_NAME` (env: `VITE_TELEGRAM_BOT_NAME`)
- **Копирование ссылок** - генерация и копирование ссылок формата `https://t.me/{BOT_NAME}?startapp={token}` (только для обычных токенов).
- **✅ НОВЫЙ Хук для админки** - `useTokensAdmin` расширен для поддержки создания персональных токенов через `assignPersonalToken`.

### ✅ Что ТАКЖЕ УЖЕ реализовано (Backend):

**Database:**
- **Таблица `access_tokens`** - ✅ ОБНОВЛЕНО! Добавлено поле `tg_id` для персональных токенов.
- **RPC функция `redeem_access_token`** - СУЩЕСТВУЕТ! Используется для обычных токенов.
- **✅ НОВЫЕ RPC функции**:
    - `assign_access_token_to_user` - для создания и немедленной активации персонального токена.
    - `find_token_by_tg_id` - для поиска неактивированных персональных токенов.
- **Таблица `user_tariffs`** - СУЩЕСТВУЕТ! Связь пользователей с тарифами.
- **Таблица `tariffs`** - СУЩЕСТВУЕТ! Справочник тарифов (T1, T2, T3, T4).
- **Таблица `courses`** - СУЩЕСТВУЕТ! Курсы для токенов.

### 🔧 Что нужно для полного MVP:

1. **Протестировать оба флоу** активации токенов (обычный и персональный).
2. **Проверить RLS политики** - убедиться, что они не мешают новым функциям.
3. **Отладить возможные ошибки** в процессе активации на стороне клиента и сервера.

---

## 9. Открытые пункты (не блокируют MVP)

1. **`expires_at`** — оставляем поле `nullable`; можно добавить позже триггер для авто-архивации.
2. **Ревокация** — если понадобится, просто `update status='revoked'`, Edge-функция отвергнет. UI-кнопку «Отозвать» легко добавить.
3. **Статистика** — в дальнейшем можно сделать дашборд «Сколько токенов выдано / использовано».
